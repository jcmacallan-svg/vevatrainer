VEVA Tabletop UI Patch (hint + hide item cards)
------------------------------------------------
This zip contains updated versions of:
- index.html (Person Search panel body: thumbnail + modal markup)
- styles.css (hide old item cards; add "Click to enlarge" overlay; modal styles; thumbnail sizing)
- patches/person_search/tabletop_renderer.js (no auto-open modal; click-to-enlarge; places items inside visible table rect)

Copy these files into your project, overwriting the existing ones.
